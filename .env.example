# Copy to .env and fill in real values. Never commit .env.
# reca-app-v2 uses the SAME Supabase project/database as reca-app (see docs/adr/ADR-002-operator-contracts.md).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_MAPBOX_TOKEN=
VITE_APP_ENV=development
