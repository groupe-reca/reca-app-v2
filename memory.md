# memory.md

Purpose: confirmed, durable project decisions only (per `docs/16-Development-Standards.md` §154). No hypotheses, no unvalidated ideas, no temporary details, no conversation history. Unconfirmed decisions belong in `plans.md`; work items belong in `tasks.md`.

Read this file (along with `tasks.md`, `plans.md`, `file-index.md`, and `CLAUDE.md`) before any significant work, and update it whenever a durable decision is confirmed.

---

## Status

The `src/` scaffold, tooling (TypeScript strict, ESLint, Prettier, Vitest, Playwright), and CI pipeline exist and are green (`pnpm check` passes, build produces no chunk-size warnings — see "Code splitting" below). The six Master UI screens (`tasks.md` T-002) are built and verified in-browser at desktop width. The Missions, Clients, Routes, Contrats, Leads, Soumissions (Quotes), Employés, Équipements, Factures (Invoices), and Paiements (Payments) modules (`tasks.md` T-003) and the Auth module (`tasks.md` T-004, real login/logout) are wired to the real shared Supabase project end-to-end. Every module's read path is verified in-browser with real network calls; write paths are verified live for Missions/Leads/Quotes/Employees/Equipment, but **Invoices/Payments' write path (record-payment + balance recalc) was explicitly left unverified live at the project owner's choice** — see "Invoices & Payments modules" below. **PostGIS is explicitly deferred, not decided** — see `docs/adr/ADR-003-postgis.md`. Vendor/infra choices with no docs default (monitoring, analytics, feature flags tooling, Storybook, deployment platform) remain open and are tracked in `plans.md` §195. `src/infrastructure/supabase/database.types.ts` has real, migration-derived types for Missions/Auth/Clients/Routes/Contrats/Leads/Quotes/Employees/Equipment/Invoices/Payments-related tables only (see "Real Supabase connection" below) — only `employee_equipment` remains untyped from the tables touched so far. Icon system and Mapbox integration are not decided/built yet, though a Mapbox token now exists in `.env` (see below).

## Code splitting (T-005)

- **This project's Vite 8 uses the Rolldown bundler, not classic Rollup** — confirmed by reading `vite`'s own `.d.ts` (`rollupOptions` is explicitly `@deprecated Use rolldownOptions instead`). The Rollup-era `manualChunks` function option is deprecated/silently ignored here; the working equivalent is `build.rolldownOptions.output.codeSplitting.groups`, an array of `{ name, test }` matchers. Don't reach for `manualChunks` from habit or older tutorials on this repo — check `node_modules/.pnpm/vite@*/node_modules/vite/dist/node/index.d.ts` if unsure, since this is exactly the kind of thing that silently no-ops instead of erroring.
- **A barrel `index.ts` re-export can defeat `React.lazy()`**: `LoginPage` was originally re-exported from `features/auth/index.ts` alongside `RequireAuth`. Because `RequireAuth` is imported eagerly (router needs it immediately), the bundler folded the whole barrel — including `LoginPage` — into the eager entry chunk, even though `LoginPage` itself was only ever referenced via `React.lazy(() => import(...))`. Rolldown's build output actually names this failure mode (`INEFFECTIVE_DYNAMIC_IMPORT`) rather than silently keeping it eager — pay attention to build warnings, they caught a real bug here. Fix: import the lazy component from its own file, and don't also re-export it from a barrel something else imports eagerly.
- **`src/routes/lazyPages.tsx`** holds all `React.lazy()` definitions, isolated from `router.tsx` specifically to avoid `react-refresh/only-export-components` warnings (a `lazy()` call assigned to a PascalCase const reads as a component export to that ESLint rule, so co-locating it with anything else in the same file makes that file "impure" for Fast Refresh purposes).
- **Two separate `<Suspense>` boundaries are needed**: one in `AppShell` around `<Outlet />` (covers every authenticated route), and one directly around the public `/login` route in `router.tsx` (it renders outside `AppShell`, so has no ancestor Suspense boundary otherwise).

## Leads module (T-003)

- **Real Lead creation was in scope, unlike Contracts/Routes** — checked `reca-app`'s real `leads.service.ts` first: creation there is a plain `insert` with no wizard-defaults lookup, no zone/geometry capture, no generated clauses. This made it safe to build the full CRUD slice (list, detail, status transitions, **and** create) in one pass rather than deferring writes to a follow-up, unlike Contracts (blocked on Zones/Invoices) or Routes (blocked on the partial-unique-index reassignment RPC).
- **Soft-delete filtering matches `reca-app`'s real pattern**: `listSummaries()`/`getById()` both filter `deleted_at is null`, mirroring the reference service. Confirmed live: both pre-existing rows in the shared `leads` table have `deleted_at` set, so the list legitimately showed "0 leads" before this session's test insert — not a query bug. Don't assume an empty list is broken without checking whether it's real soft-deleted state first (verified here via a direct authenticated REST query outside the app, using the real admin credentials, before concluding the UI was correct).
- **A real test lead now lives in the shared DB**: "Test RECA-App-V2 QA" / LEAD-000011, created live through `LeadCreatePage` to verify the write path end-to-end (create → detail redirect → status update NOUVEAU→CONTACTÉ → reflected in list). Its message field says it's a QA record and can be removed. There's no delete UI in this app yet, so removing it (if ever wanted) means a direct SQL/dashboard soft-delete — this was a deliberate, user-approved live write (see `tasks.md` T-003 Leads), not an accidental mutation.
- **`exactOptionalPropertyTypes: true` means optional fields need conditional spreads, not `?? undefined`**: `CreateLeadInput`'s optional string fields (`telephone`, `courriel`, etc.) can't be assigned `string | undefined` even via `||`/`??` — the target type only accepts `string` or the key being absent. `LeadCreatePage`'s submit handler builds the payload with `...(value ? { key: value } : {})` per optional field instead. Applies to any future form mapping optional strings onto a `CreateXInput`-style type under this repo's strict tsconfig.

## Quotes module (T-003)

- **"Link to client" matches `reca-app`'s real behavior, not the docs' aspirational `ConvertQuoteToClient`**: `docs/16-Development-Standards.md`'s list of critical multi-step transactional mutations names `ConvertQuoteToClient` as an example, implying it creates a client (and possibly contract) from a quote. But `reca-app`'s actual `convertQuoteToClient()` service function is a plain `update({client_id, statut: 'acceptee'})` — it assumes the client already exists (created separately) and only links + accepts. There is no client-creation or contract-creation logic anywhere in the reference app's quote conversion path. `QuoteDetailPage`'s "Lier au client" picker replicates exactly this real, simpler capability (backed by `useClients()`), and says so explicitly in its UI copy so nobody mistakes it for a real conversion wizard. **Don't build a real transactional quote→client→contract RPC without a fresh scoping decision** — that would be inventing a capability the legacy system never had, same reasoning as the Contracts/Routes write scope-outs below.
- **Money**: `quotes.montant`/`taxes`/`total` are decimal-dollar columns (like `contracts.prix`), converted to `MoneyCents` at the mapper boundary. `total` is recomputed as `montant + taxes` on every create/update rather than trusted from client input, matching `reca-app`'s real service (there's no DB check constraint enforcing the relationship, so the app layer must keep it consistent).
- **Soft-delete filtering matches the established pattern**: `listSummaries()`/`getById()` filter `deleted_at is null`, same as Leads/Clients. Confirmed live: the one pre-existing quote in the shared DB is soft-deleted, so "0 soumissions" before this session's test insert was genuine real state, not a bug — verified via a direct authenticated REST query before concluding the UI was correct, same verification method used for Leads.
- **A real test quote now lives in the shared DB**: SOUM-000008, created live through `QuoteCreatePage`, then moved BROUILLON → ENVOYÉE → (via linking) ACCEPTÉE, linked to the real client "Jean Test". Its notes field says it's a QA record. No delete UI exists yet for quotes either — same situation as the Leads test record.
- **Cross-table lookups use separate per-table queries, not embedded selects** — same reasoning as Missions/Routes: the hand-written `Database` type has no `Relationships` metadata for supabase-js to type embeds safely. `quote.repository.ts` batches `leads`/`clients` lookups by unique ID across the whole list rather than N+1 querying per row.

## Invoices & Payments modules (T-003)

- **`RecordPayment` matches `reca-app`'s real non-transactional behavior, not the docs' aspirational transactional RPC**: `docs/16-Development-Standards.md`'s list of critical multi-step transactional mutations names `RecordPayment` as an example. But `reca-app`'s real `payments.service.ts` implements it as 3 sequential client-side Supabase calls — insert the payment row, sum all non-deleted payments for the invoice, then update `invoices.solde`/`statut` — with no Postgres RPC or DB transaction wrapping them. `SupabasePaymentRepository.create()` (`src/features/payments/infrastructure/payment.repository.ts`) mirrors this exactly via a `recalcInvoiceBalance()` helper. Same reasoning as Quotes' non-transactional "link to client": match what the reference system actually does, don't invent a more correct-looking implementation that the legacy app never had. **If this is ever made truly transactional (a real risk — a failed second/third call after the insert would leave `solde` stale), that's a deliberate future upgrade, not something quietly fixed here.**
- **Invoice status recalc logic, mirrored exactly**: `totalPaid <= 0` reverts `payee`/`partiellement_payee` back to `envoyee`; `solde <= 0` (fully paid) → `payee`; otherwise → `partiellement_payee`. Copied from `reca-app`'s real `recalcInvoiceBalance()`, not reinvented.
- **No line-items table, no auto-generation from a contract's payment schedule** — `invoices` are flat `sous_total`/`tps`/`tvq`/`total`/`solde` numeric columns (same shape as Quotes' `montant`/`taxes`/`total`), and there is no billing-frequency/payment-terms field anywhere on `contracts` in the real schema. Invoices are created manually in `reca-app`, optionally linked to a `contrat_id` — same here. Don't build auto-invoicing without a fresh scoping decision; it doesn't exist in the reference system.
- **Payments have no direct client/contract FK** — only `facture_id → invoices.id`. To show a payment's contract, you go through its invoice's `contrat_id`. `PaymentListPage` links back to the invoice, not a contract, matching this real shape.
- **No standalone payment-create route** — payments are only ever recorded from `InvoiceDetailPage` via the embedded `RecordPaymentForm` component, matching the real app's UX (there's no generic "record a payment against nothing in particular" flow in `reca-app` either).
- **Live write test explicitly declined for this module** — unlike Leads/Quotes/Employees/Equipment, the project owner chose not to have a test invoice+payment created live this pass. The create/recalc logic is typecheck/lint/test-verified and follows the same pattern as every other verified write path, but the balance-recalculation sequence itself has not been exercised against real data. Don't claim it's "verified live" — only the read path (50 real invoices, correct money math, correct contract linking) was confirmed in-browser.

## Employees & Equipment modules (T-003)

- **Employees has no status enum, only a boolean** — unlike every other status-bearing table built so far (Leads, Quotes, Missions, Equipment), `employees.actif` is a plain boolean in the real schema, not a multi-value `statut` column. Don't invent a richer employee-status model than the DB actually has; `EmployeeDetailPage`'s status panel is a single toggle, not a status-order button list.
- **`employees.role` is free text, not an enforced enum** — no CHECK constraint in the real migration. `employeeRoles = ['Administrateur', 'Employé']` (matching `reca-app`'s real `EMPLOYEE_ROLES` constant) is used as a `<select>` in the create form for UX consistency, but the column accepts any string — don't add a CHECK constraint or app-level validation that would reject a value the real DB happily accepts.
- **`equipments.statut` is excluded from the create/edit schema on purpose** — matches `reca-app`'s real `equipment.schema.ts`: status is only ever changed via a dedicated status-update action, never as a form field alongside name/category/etc. New equipment always starts at the column's real DB default (`disponible`). Don't add a status dropdown to `EquipmentCreatePage` — that would diverge from the reference app's actual UX split between "describe the equipment" and "change its operational state."
- **`employee_equipment` (M:N employee↔equipment assignment) was not built** — `reca-app` has a separate service for it, but wasn't needed for this pass since missions/routes already reference employees/equipments directly via `operator_id`/`equipment_id`. A real "assign equipment to an employee" UI is future scope, not silently covered by what's here.
- **Real test records created live and left in the shared DB**: a test employee ("Test RECA-App-V2 QA", toggled to inactive) and a test equipment item ("Test RECA-App-V2 QA", EQ-009, moved to `entretien`) — same situation as the Leads/Quotes test records: no delete UI exists yet, both are clearly labeled as QA records in their notes/entretien fields.

## Contrats module (T-003)

- **Real contract _creation_ is out of scope, and deliberately not attempted** — checked `reca-app`'s real `contracts.service.ts` before touching `ContractWizardPage`: real creation involves satellite property/zone capture, auto-generated legal clauses (`generateClauses`), a settings-driven wizard-defaults lookup (`settings.contract_wizard_defaults`), soft-delete/upsert zone and photo syncing, and automatic invoice-schedule generation from payment terms. `ContractWizardPage`'s existing 3-step mock (client/service/confirmation) covers none of that. Wiring its "Créer le contrat" button to a real `insert` would produce a contract missing everything the real system actually requires — worse than an honest mock. **Don't wire the mock wizard to a real write without first scoping Zones + Invoices** — that's real follow-up work, not a quick fix.
- Only the read side (list + detail) was built this pass, following the same repository/mapper/hooks pattern as Missions/Clients/Routes. `ContractDetail` cross-links to its client, and `ClientDetailPage`/`RouteDetailPage` now link their contract rows to it — verified the full round trip live.

## Routes module (T-003)

- **Read-only on purpose** — `reca-app`'s real write logic for routes is more involved than a plain `UPDATE`: `assignContractToRoute` must first soft-delete any existing active `route_contracts` row for that contract before inserting a new one (a contract can only be actively assigned to one route at a time — enforced by a partial unique index, `route_contracts_contract_active_key`), and reordering goes through a dedicated `reorder_route_contract` Postgres RPC, not a direct column update. Building a shallow version of either without replicating that logic would produce a subtly incorrect result under any partial-index or transaction assumptions the RPC handles. Deferred to a follow-up rather than rushed.
- **Write RLS matches Missions' original (pre-operator-exception) shape, not Clients'**: `routes` and `route_contracts` are `administrateur`-only for INSERT/UPDATE with no operator carve-out (`reca-app/supabase/migrations/20260722020000_routes_v2.sql`) — unlike `missions`/`mission_items`, which later gained an assigned-operator exception. Don't assume all "assigned resource" tables follow the same write-RLS shape; check each one.
- **`contracts.adresse_geocodee`** was added to `database.types.ts` specifically because `reca-app`'s real `routeContracts.service.ts` selects it to show each stop's address in route order — cross-checked, not guessed.

## Clients module (T-003)

- **No "solde"/balance concept exists in the reference app** — checked `reca-app`'s real `ClientContractsCard.tsx`/`ClientInvoicesCard.tsx` before building: the real client detail page shows a contracts total and a separate invoices total, never a combined balance figure. T-002's mock `ClientDetailPage` had invented a fictional "Solde"/"Solde dû" stat that doesn't correspond to anything real. It's been replaced with a real "Valeur des contrats" figure computed from the actual `contracts` table. Don't reintroduce a "balance" stat until the Invoices/Payments module is actually built and a real calculation exists to back it.
- **`clients.statut` needs no anti-corruption mapping** — unlike Mission's French `statut` values, `clients.statut` (`actif`/`inactif`) reads fine directly as app-level values. Contrast with `missions.statut`, which does need `mapLegacyMissionStatus()` — don't assume every legacy status column needs a translation layer; check each one.
- **Money unit mismatch**: `contracts.prix` is stored as a decimal dollar amount (`numeric(12,2)`) in the DB, not integer cents. The mapper (`client.mapper.ts`) converts to `MoneyCents` (`Math.round(prix * 100)`) at the boundary so the rest of the app only ever sees cents, per `docs/16-Development-Standards.md` §25. Apply the same conversion if `contracts.prix` is read anywhere else (e.g. a future Contracts module).
- **`ClientListPage` (`/clients`) didn't exist before this** — T-002 only built the detail page (Master UI screen 3 only needed one commercial-record example). Added to match the list+detail pattern already established for Missions.

## Auth (T-004)

- **Real role model, not the docs' aspirational one**: `docs/05-Authentication-Roles-Permissions.md` §10–21 describes a full roles/permissions/user_roles/role_permissions model (7 roles, resource.action permission keys). The actual shared DB implements only the "modèle simplifié temporaire" the same doc explicitly sanctions in §22: a single `public.users.role` column with 3 values (`administrateur`, `employe`, `operateur` — `reca-app/supabase/migrations/20260709143631_users.sql` + `20260723010000_users_role_operateur.sql`), and RLS today is "any authenticated user may read; only administrateur may write" (`reca-app/supabase/migrations/20260709143718_rls_policies.sql`), plus a narrower operator-write exception on `mission_items`. `src/domain/session.ts` (`AppSession`, `Role`, `canWrite()`, `isOperator()`) matches this real model. **Do not build the full permission-key matrix (`hasPermission()`, `RequirePermission`, resource.action keys) until it's actually backed by real RLS policies** — a frontend-only permission system with no matching DB enforcement is exactly the false security docs §2 warns against ("masquer un bouton n'est pas une sécurité").
- **Session resolution**: `src/app/SessionContext.tsx` (`SessionProvider`/`useSession()`) listens to Supabase Auth state and joins it with the matching `public.users` row (RLS: `users_select_self_or_admin` — a user can always read their own row) to build the `AppSession`. A suspended (`actif = false`) account is blocked at `RequireAuth`, not just hidden.
- **Route protection**: `/login` is the only public route; everything else is nested under `RequireAuth` (`src/features/auth/components/RequireAuth.tsx`) inside the router, per `docs/05-Authentication-Roles-Permissions.md` §43. This is a route guard, not real security — RLS remains the actual authority, per docs §2.
- **No public sign-up**: matches the docs' admin-invite model (§28, §31). There's no admin-side invite UI yet either — provisioning real accounts today means using the Supabase dashboard directly.
- **Verified, not assumed**: real `POST https://ynsuxctqsvusbgcudcno.supabase.co/auth/v1/token?grant_type=password` requests confirmed via network inspection (400 for bad credentials, generic UI message shown per docs §30 — no raw Supabase response leaked). `tests/e2e/smoke.spec.ts` was rewritten because it had gone stale after T-002/T-003 changed the dashboard heading and added the login redirect — it wasn't part of `pnpm check` so nobody would have noticed until it was manually run. **`pnpm test:e2e` is not part of `pnpm check`** — remember to run it manually after router/page changes, or it can silently rot again.
- **`mobile-safari` Playwright project doesn't run in this dev sandbox** (missing Windows `msvcp140_1.dll`, unrelated to the app) — only `chromium` has been verified locally. Not a CI concern unless CI runs on a similarly bare Windows image.
- **A real `administrateur` account exists and was used to verify the full stack end-to-end** (project owner provided the credentials directly in chat; never written to any file, never logged — provisioning/rotating it is the project owner's call, not recorded here). Confirmed real data flows correctly through the whole chain built so far: login → `public.users` role resolution → `useMissions()`/`useMission()` → dashboard/list/detail pages, including the `mission_items → contracts → clients` join showing real client names and contract numbers. This validates the Missions repository, the anti-corruption status mapper, and the Auth session flow all work correctly against production-shaped data, not just against empty RLS-filtered results.

## Real Supabase connection (T-003)

- **Live credentials**: `.env` (gitignored, never committed) is populated with the real shared project's `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, and `VITE_MAPBOX_TOKEN`, sourced from `.input/.env` (also gitignored) at the project owner's direction. Project ref: `ynsuxctqsvusbgcudcno`.
- **RLS blocks anon reads on Missions tables — this is expected, not a bug**: every RLS policy on `missions`/`mission_items`/`mission_events`/`mission_notes` in `reca-app`'s migrations is `for select to authenticated` only. The anon key gets valid `200` responses with empty arrays (confirmed via real network requests), never actual rows, until real Supabase Auth sign-in is implemented. Don't "fix" this by loosening RLS or adding anon policies — that would be a real security decision requiring explicit sign-off, not a bootstrapping workaround.
- **Schema introspection is also blocked for anon/no-DB-password access**: this project's PostgREST gateway restricts both the root OpenAPI endpoint and `OPTIONS`-based column introspection to `service_role` only. `database.types.ts`'s Missions-related types were instead hand-derived by reading `reca-app`'s actual migration files at `C:/var/www/html/reca-app/supabase/migrations/` (table names were also cross-confirmed live via anon-key REST probing). This is a legitimate one-time bridge, not the mandated method — see `plans.md` "Real database.types.ts generation" for what would supersede it (a DB connection string or service_role key, used once locally).
- **`Database` type must include `Relationships: []` per table** — supabase-js's `GenericTable` constraint requires it even when unused (no embedded-relation selects), otherwise every query silently infers as `never`/`unknown` instead of erroring, which is a confusing failure mode to hit blind. Found this the hard way; keep it when adding more tables to `database.types.ts`.
- **Anti-corruption layer for Mission status**: `reca-app`'s `missions.statut` column uses French lowercase values (`planifiee`, `en_cours`, `terminee`, `terminee_avec_anomalies`, `annulee`) — different from the generic `PLANNED/READY/IN_PROGRESS/...` example in `docs/03-Application-Architecture.md` §18. `src/domain/missionStatus.ts` now defines the real 5-value app-level `MissionStatus` plus `mapLegacyMissionStatus()`/`mapMissionStatusToLegacy()` translating both directions, per the anti-corruption layer mandated in `docs/03-Application-Architecture.md` §52. There is no `READY`/`PAUSED` state in the real schema — don't reintroduce those without checking the DB again. Pause/resume exist as `mission_events` types but have no corresponding `missions.statut` value (a mission stays `en_cours` while "paused" — not modeled as a status transition yet).
- **Mission write RLS, confirmed real** (`reca-app/supabase/migrations/20260725010000_missions_operator_write_access.sql`): `missions` and `mission_items` UPDATE is allowed for `administrateur` **or** the employee assigned as that mission's `operator_id` (via `employees.user_id = auth.uid()`); INSERT stays admin-only for both tables. There is genuinely no "reassign operator/equipment after creation" capability anywhere in `reca-app` — `missions.service.ts` only sets `operator_id`/`equipment_id` at `createMission()`. Don't build a fake "Assigner" mutation; the T-002 placeholder button for it was removed rather than wired to a non-existent capability.
- **Status-transition mutations mirror `reca-app`'s real code, not invented**: `SupabaseMissionRepository.updateStatus()` copies the exact patch shape (`statut` + `heure_debut` on start + `heure_fin` on completion) and the immediately-following `mission_events` insert from `reca-app/src/features/missions/services/missions.service.ts#updateMissionStatus`. Verified live: a résidence status change round-tripped through a real write + refetch (no optimistic UI), then was reverted to leave shared dev data untouched. The mission-level `Démarrer` transition wasn't clicked live (no `PLANIFIÉE` mission exists in the shared dev data right now to test non-destructively) — only visually confirmed the correct buttons render per status.

## Master UI decisions (T-002)

- **Design tokens**: implemented literally per `docs/01-Design-System.md` §6–8 in `src/styles/index.css` — brand/status color tokens, dark (primary) and light theme CSS variable sets under `[data-theme]`, Manrope typography scale, radius/shadow scale. Theme defaults to dark (per §5.1) with a working toggle (`src/app/useTheme.ts`) persisted to `localStorage`.
- **Navigation structure**: sidebar sections/items and mobile bottom-nav items in `src/layouts/navigation.ts` match the exact order given in `docs/02-Information-Architecture.md`. Do not reorder without checking that doc.
- **Mobile is handled as a responsive shell, not a separate route tree**: `AppShell` swaps sidebar → bottom nav at the `lg` (1024px) breakpoint via Tailwind utilities (`hidden lg:flex` / `lg:hidden`), rather than building fully separate mobile route components. This was a scope call for the first pass — acceptable per `docs/16-Development-Standards.md` §75 (structural responsive differences may use dedicated markup blocks, not necessarily separate components), but revisit if a screen needs mobile-specific data/behavior beyond layout.
- **No icon library adopted** — nav and UI are currently text-only. Docs don't specify an icon system; this needs a real decision (and likely real brand assets per `docs/01-Design-System.md` §4.1) before one is picked, not an invented icon set.
- **Map placeholders**: Dashboard and Mission detail have explicit "carte à venir" placeholders instead of Mapbox, since Mapbox needs an API token decision not yet made. Don't silently wire up a real map without that decision.
- **Mock data**: `src/features/{missions,clients}/mocks.ts` are placeholder fixtures, clearly commented as not-Supabase-backed. Real data requires `database.types.ts` to be regenerated first (T-001).

## Bootstrap decisions

- **Package manager**: pnpm. `docs/adr/ADR-001-package-manager.md`.
- **Operator-contract / database strategy**: `reca-app-v2` uses the **same Supabase database/project as `reca-app`** — confirmed directly by the project owner ("tout est géré par Supabase, on utilise la même base de donnée que reca-app"). There is no separate `@reca/contracts` package and no cross-repo type-generation pipeline; the shared Postgres schema (tables, RPCs, migrations) _is_ the integration contract with `reca-operateur`. Legacy/foreign fields still go through the anti-corruption layer per `docs/03-Application-Architecture.md` §52. `docs/adr/ADR-002-operator-contracts.md`.
- **DB type generation**: Supabase CLI (`supabase gen types typescript`), output committed to `src/infrastructure/supabase/database.types.ts`, never hand-edited (per `docs/16-Development-Standards.md` §82).
- **Branded types**: adopt lightweight nominal typing for IDs (`MissionId`, `ContractId`, etc.) per `docs/16-Development-Standards.md` §20 — reversible, low-risk TypeScript ergonomics choice, no ambiguity in the docs.
- **Git conventions**: adopted as proposed in `docs/16-Development-Standards.md` §139–141 — branch prefixes `feature/*`, `fix/*`, `migration/*`, `docs/*`, `hotfix/*`; commit messages in the `type(scope): summary` style shown there.
- **Test coverage policy**: risk-based priority (critical business rules, transactions, permissions, mappers, sync, finance) rather than a blanket numeric coverage threshold, per `docs/16-Development-Standards.md` §132.
- **i18n scope**: fr-CA is the only shipped language initially; string/translation keys are structured from the start so en-CA can be added later without rework, per `docs/16-Development-Standards.md` §121.
- **PostGIS**: NOT decided — deferred. Default behavior until revisited: geometry stored as GeoJSON/JSONB. See `docs/adr/ADR-003-postgis.md`. Do not treat this as settled.
- **CI provider**: GitHub Actions (`.github/workflows/ci.yml`), running `format:check`, `lint`, `typecheck`, `test`, `build` on push to `main` and on pull requests. Adopted because the repo is already hosted on GitHub — this uses infrastructure already in place, not a new vendor commitment, so it wasn't treated as needing the same owner sign-off as monitoring/analytics/deployment choices. Deployment platform (where the built app actually runs) is a separate, still-open decision.
- **Exact tool versions** (locked via `pnpm-lock.yaml`): React 19, Vite 8, **TypeScript 5.9** (not 7 — `typescript-eslint` 8.66 doesn't support TS 7.0 yet, so installing `typescript@latest` breaks lint; pin to `^5` until that changes), Tailwind CSS 4, TanStack Query 5, React Router 7, Zod 4, React Hook Form 7, Vitest 4, `@playwright/test` 1.62.
- **ESLint/Prettier configuration**: `eslint.config.js` (flat config) using `typescript-eslint` strict + stylistic type-checked rules, `eslint-plugin-react-hooks`, `eslint-plugin-jsx-a11y`, `eslint-plugin-react-refresh`; explicit rules for `no-explicit-any: error`, `no-non-null-assertion: error`, `no-floating-promises: error`, `consistent-type-imports: error`, and a `no-restricted-imports` rule blocking `createClient` from `@supabase/supabase-js` everywhere except `src/infrastructure/supabase/client.ts`. Prettier config (`.prettierrc.json`): no semicolons, single quotes, trailing commas, 90 print width, `prettier-plugin-tailwindcss` for class sorting.
- **Repo structure**: adopted as documented (`docs/03-Application-Architecture.md` §9, `docs/16-Development-Standards.md` §7–8) — `src/{app,config,domain,features,infrastructure,routes,styles,test}`, `supabase/{functions,migrations,seed,tests}`, `tests/{e2e,integration,fixtures}`, `docs/adr/`. Subfolders are created only when they represent a real responsibility, not pre-scaffolded wholesale.

## Confirmed decisions (from official documentation)

- **Product name**: RECA — Centre des opérations. Technical project name: RECA App V2. (`docs/00-Vision.md` §2)
- **Repository name**: `reca-app-v2`. (`docs/00-Vision.md` §2)
- **Three-repository boundary** (non-negotiable):
  - `reca-app-v2` — new application, new source of truth for the frontend.
  - `reca-app` — legacy app, functional/schema reference only, never modified without explicit request, never a runtime dependency of `reca-app-v2`.
  - `reca-operateur` — field/operator app, never modified without explicit request, never imported directly by `reca-app-v2`; the two communicate only via versioned data contracts.
    (`docs/00-Vision.md` §6, `docs/03-Application-Architecture.md` §4)
- **Data source of truth**: the existing Supabase database may be retained and adapted; a second database must not be created merely to avoid understanding the existing one. Changes must be documented, migratable, additive where possible, RLS-secured, and tested. (`docs/00-Vision.md` §20.1)
- **Recommended (not yet locked) tech stack**: React, Vite, TypeScript strict, React Router, TanStack Query, React Hook Form, Zod, Tailwind CSS on the frontend; Supabase (Postgres, Auth, Storage, Realtime, Edge Functions, PostGIS as needed) on the backend; Mapbox GL JS + Turf.js for mapping; Vitest, Testing Library, Playwright for testing. Exact versions to be locked at bootstrap. (`docs/03-Application-Architecture.md` §7, `docs/16-Development-Standards.md` §3)
- **Architecture style**: feature-first modules with internal Domain → Application → Infrastructure → Presentation layering; dependencies point toward the domain only. (`docs/03-Application-Architecture.md` §5–6, `docs/16-Development-Standards.md` §9–11)
- **Method of construction**: Vision → Design System → Information Architecture → Application Architecture → Data Architecture → Business modules → Master UI → Roadmap → Sprints → Implementation → Tests → Migration. Code must not begin before the required foundations are defined. (`docs/00-Vision.md` §33)

## Source-of-truth hierarchy (in case of contradiction)

1. Explicit recent decision from the project owner
2. Official RECA App V2 documentation (`docs/`)
3. `memory.md` (this file)
4. `plans.md`
5. RECA App V2 code and tests
6. `reca-operateur` documentation
7. Confirmed behavior of the legacy `reca-app`
8. Legacy app memory
9. Old mockups
10. Developer assumption

(`docs/00-Vision.md` §29)
